# MCP Server: Azure Docs (Stub)

Search across Microsoft Learn / Docs for authoritative content and return curated results.

## Endpoints
- `GET /health` — server status
- `GET /api/search?q=...` — stub; to be implemented

## Setup
```powershell
cd servers/mcp-azure-docs
npm install
copy .env.sample .env
npm run start
```
