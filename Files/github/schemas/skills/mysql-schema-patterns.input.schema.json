{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MySQL Schema Patterns Skill Input",
  "description": "Input schema for mysql-schema-patterns skill",
  "type": "object",
  "properties": {
    "patterns": {
      "type": "array",
      "description": "Schema patterns to demonstrate",
      "items": { "type": "string" }
    }
  },
  "required": ["patterns"]
}
