-- Azure SQL Stored Procedure Template
-- {{name}}
-- {{description}}
-- Generated by AgenticCoder

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

IF EXISTS (SELECT * FROM sys.procedures WHERE name = '{{name}}')
    DROP PROCEDURE [dbo].[{{name}}];
GO

CREATE PROCEDURE [dbo].[{{name}}]
    {{#each parameters}}
    @{{name}} {{sqlType}}{{#if output}} OUTPUT{{/if}}{{#if default}} = {{default}}{{/if}}{{#unless @last}},{{/unless}}
    {{/each}}
AS
BEGIN
    SET NOCOUNT ON;
    
    {{#if transaction}}
    BEGIN TRY
        BEGIN TRANSACTION;
    {{/if}}
    
    {{#if validation}}
    -- Input validation
    {{#each validation}}
    IF {{condition}}
    BEGIN
        {{#if raiseError}}
        RAISERROR('{{message}}', 16, 1);
        RETURN -1;
        {{else}}
        THROW 50001, '{{message}}', 1;
        {{/if}}
    END
    {{/each}}
    {{/if}}
    
    {{body}}
    
    {{#if transaction}}
        COMMIT TRANSACTION;
    END TRY
    BEGIN CATCH
        IF @@TRANCOUNT > 0
            ROLLBACK TRANSACTION;
        
        DECLARE @ErrorMessage NVARCHAR(4000) = ERROR_MESSAGE();
        DECLARE @ErrorSeverity INT = ERROR_SEVERITY();
        DECLARE @ErrorState INT = ERROR_STATE();
        
        RAISERROR(@ErrorMessage, @ErrorSeverity, @ErrorState);
        RETURN -1;
    END CATCH
    {{/if}}
    
    RETURN 0;
END
GO

{{#if permissions}}
-- Grant permissions
{{#each permissions}}
GRANT {{permission}} ON [dbo].[{{../name}}] TO [{{role}}];
GO
{{/each}}
{{/if}}
