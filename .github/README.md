# .github

This folder contains GitHub metadata and repository-level assets.

- `workflows/`: CI pipelines
- `schemas/`: JSON schemas for agents, artifacts, and contracts
- `agents/`, `skills/`, `scenarios/`, `templates/`: Plan-C target structure (stubs)
